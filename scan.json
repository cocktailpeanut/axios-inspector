{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "shell": "{{which('bash')}}",
        "conda": {
          "skip": true
        },
        "env": {
          "PINOKIO_PLATFORM": "{{platform}}"
        },
        "message": "bash ./scan.sh \"{{kernel.homedir}}\""
      }
    }
  ]
}
